# Migration `20200703013952-init`

This migration has been generated by Harshit Pant at 7/3/2020, 1:39:52 AM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "public"."Transcriber" (
"email" text  NOT NULL ,
"firstName" text  NOT NULL ,
"id" text  NOT NULL ,
"lastName" text  NOT NULL ,
    PRIMARY KEY ("id"))

CREATE TABLE "public"."TranscriberCompetency" (
"competencyId" text  NOT NULL ,
"id" text  NOT NULL ,
"level" integer  NOT NULL ,
"transcriberId" text  NOT NULL ,
    PRIMARY KEY ("id"))

CREATE TABLE "public"."Competency" (
"id" text  NOT NULL ,
"name" text  NOT NULL ,
"type" text  NOT NULL ,
    PRIMARY KEY ("id"))

CREATE UNIQUE INDEX "Transcriber.email" ON "public"."Transcriber"("email")

CREATE UNIQUE INDEX "TranscriberCompetency.transcriberId_competencyId" ON "public"."TranscriberCompetency"("transcriberId","competencyId")

CREATE UNIQUE INDEX "Competency.name" ON "public"."Competency"("name")

ALTER TABLE "public"."TranscriberCompetency" ADD FOREIGN KEY ("transcriberId")REFERENCES "public"."Transcriber"("id") ON DELETE CASCADE  ON UPDATE CASCADE

ALTER TABLE "public"."TranscriberCompetency" ADD FOREIGN KEY ("competencyId")REFERENCES "public"."Competency"("id") ON DELETE CASCADE  ON UPDATE CASCADE
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration ..20200703013952-init
--- datamodel.dml
+++ datamodel.dml
@@ -1,0 +1,34 @@
+generator client {
+  provider = "prisma-client-js"
+}
+
+datasource db {
+  provider = "postgresql"
+  url = "***"
+}
+
+model Transcriber {
+  id               String                       @id @default(cuid())
+  email            String                       @unique
+  firstName        String
+  lastName         String
+  competencies     TranscriberCompetency[]
+}
+
+model TranscriberCompetency {
+  id            String      @id @default(cuid())
+  transcriber   Transcriber @relation(fields: [transcriberId], references: [id])
+  transcriberId String
+  competency    Competency  @relation(fields: [competencyId], references: [id])
+  competencyId  String
+  level         Int
+  @@unique([transcriberId, competencyId])
+}
+
+model Competency {
+  id                          String                       @id @default(cuid())
+  type                        String
+  name                        String                       @unique
+  transcriberCompetencies     TranscriberCompetency[]
+}
+
```


